version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 24
    commands:
      - npm install -g yarn
      - yarn
      - yarn setup
  build:
    commands:
      - yarn build
artifacts:
  files:
    - '**/*'
  base-directory: client/dist
