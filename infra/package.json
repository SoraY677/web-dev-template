{
  "name": "infra",
  "version": "0.1.0",
  "license": "MIT",
  "bin": {
    "main": "dist/bin/main.js"
  },
  "scripts": {
    "watch": "nodemon",
    "test": "jest",
    "clean": "rimraf cdk.out",
    "bootstrap": "yarn clean && cdk bootstrap --context",
    "deploy": "yarn clean && cdk deploy --all --context",
    "destroy": "yarn clean && cdk destroy --all --context"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "aws-cdk": "2.1019.2",
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.201.0",
    "constructs": "^10.0.0",
    "dotenv": "^17.2.1"
  }
}
