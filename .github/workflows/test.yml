name: All Static Test

on:
  push:
    paths:
      - '*'
    branches-ignore:
      - "release"

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3.1.1
        with:
          node-version: v22.17.0	

      - name: Init
        run: npm install

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build
